#!/usr/bin/python

# Import needed function from setuptools
from setuptools import *

# Create proper setup to be used by pip
setup(name='havey',
      version='0.3',
      description='Create a nice command line interface.',
      author='nadjib',
      packages=['havey'])
